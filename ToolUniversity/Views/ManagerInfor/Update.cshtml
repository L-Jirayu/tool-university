@model IEnumerable<ToolUniversity.Models.db.ManagerInfor>;
@{
    ViewData["Title"] = "Update";
}

<h1>Update and Delete</h1>
@if (Model.Count() <= 0)
{
    <div class="alert alert-danger">No data found</div>
}
else
{
    <table id="mytable">
        <thead>
            <tr>
                <th>Manager ID</th>
                <th>Manager Name</th>
                <th>Update</th>
                <th>Remove</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.ManagerId</td>
                    <td>@item.Name</td>
                    <td>
                        <a class="btn btn-warning"
                           asp-controller="ManagerInfor"
                           asp-action="ShowUpd"
                           asp-route-id="@item.ManagerId"
                           onclick="return confirm('Do you want to update: @item.Name?')">
                            Update
                        </a>
                    </td>
                    <td>
                        <a class="btn btn-danger"
                           asp-controller="ManagerInfor"
                           asp-action="DelDB"
                           asp-route-id="@item.ManagerId"
                           onclick="return confirm('Do you want to remove: @item.Name?')">
                            Remove
                        </a>
                    </td>
                </tr>
            }
        </tbody>

    </table>
}
