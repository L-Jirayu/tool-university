@model IEnumerable<ToolUniversity.Models.db.OfficerInfor>;
@{
    ViewData["Title"] = "Update";
}

<h1>Update and Delete</h1>
@if (Model.Count() <= 0)
{
    <div class="alert alert-danger">No data found</div>
}
else
{
    <table id="mytable">
        <thead>
            <tr>
                <th>Officer ID</th>
                <th>Officer Name</th>
                <th>Update</th>
                <th>Remove</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.OfficerId</td>
                    <td>@item.Name</td>
                    <td>
                        <a class="btn btn-warning"
                           asp-controller="OfficerInfor"
                           asp-action="ShowUpd"
                           asp-route-id="@item.OfficerId"
                           onclick="return confirm('Do you want to update: @item.Name?')">
                            Update
                        </a>
                    </td>
                    <td>
                        <a class="btn btn-danger"
                           asp-controller="OfficerInfor"
                           asp-action="DelDB"
                           asp-route-id="@item.OfficerId"
                           onclick="return confirm('Do you want to remove: @item.Name?')">
                            Remove
                        </a>
                    </td>
                </tr>
            }
        </tbody>

    </table>
}
